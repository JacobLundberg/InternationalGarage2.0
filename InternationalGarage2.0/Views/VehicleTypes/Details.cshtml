@model InternationalGarage2_0.Models.VehicleType

@{
    ViewData["Title"] = "Vehicle Type Details";
}

<h1>Vehicle Type Details</h1>

<div>
    <h4>VehicleType</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
    <a class="btn btn-sm btn-primary" asp-action="Index"><i class="fas fa-undo-alt"></i> Back to Vehicle Type List</a>
</div>
